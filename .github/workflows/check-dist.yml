name: Check Dist

on:
  push:
    branches: [main, master]
    paths-ignore: '**.md'
  pull_request:
    branches: [main, master]
    paths-ignore: '**.md'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check-dist:
    name: Check dist
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24.x

      - name: Install dependencies
        run: |
          for i in {1..3}; do
            npm ci && break || sleep 10
          done

      - name: Build
        run: npm run build

      - name: Compare dist
        run: |
          if [ "$(git diff --name-only | grep dist/ | wc -l)" -gt "0" ]; then
            echo "🔍 The following files have uncommitted changes:"
            git diff --name-only | grep dist/
            echo ""
            echo "📦 Dist folder size:"
            du -sh dist/
            echo ""
            echo "⚠️ Files changed since last commit:"
            git diff --stat -- 'dist/**'
            echo ""
            echo "❌ Detected uncommitted changes after build."
            echo "Please run 'npm run build' and commit the changes."
            exit 1
          fi
          echo "✅ No uncommitted changes detected in dist/"
